package com.csniico.restart.instructor.progress.repository;

import com.csniico.restart.instructor.progress.entity.GradebookReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GradebookReportRepository extends JpaRepository<GradebookReport, Long> {}
